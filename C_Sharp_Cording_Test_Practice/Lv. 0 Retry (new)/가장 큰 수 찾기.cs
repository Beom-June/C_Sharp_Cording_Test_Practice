using System;

/* Retry
 * 정수 배열 array가 매개변수로 주어질 때, 
 * 가장 큰 수와 그 수의 인덱스를 담은 배열을 return 하도록 solution 함수를 완성해보세요.
*/
namespace C_Sharp_Cording_Test_Practice
{
    /*
    public class Solution
    {
        public int[] solution(int[] array)
        {
            int _maxValue = int.MinValue;
            int _maxIndex = -1;

            // 배열을 순회하며 최대값과 그 인덱스를 찾습니다.
            for (int i = 0; i < array.Length; i++)
            {
                if (array[i] > _maxValue)
                {
                    _maxValue = array[i];
                    _maxIndex = i;
                }
            }

            return new int[] { _maxValue, _maxIndex };
        }
    }
    class 가장_큰_수_찾기
    {
        static void Main(string[] args)
        {
            Solution _solution = new Solution();

            int[] _arr01 = { 1, 8, 3 };
            int[] _arr02 = { 9, 10, 11, 8 };

            Console.WriteLine(String.Join(" ", _solution.solution(_arr01)));
            Console.WriteLine(String.Join(" ", _solution.solution(_arr02)));
        }
    }
    */
}
