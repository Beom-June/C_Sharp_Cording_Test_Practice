using System;

/* Retry
 * 문자열 my_string과 정수 num1, num2가 매개변수로 주어질 때, 
 * 
 * my_string에서 인덱스 num1과 인덱스 num2에 해당하는 문자를 바꾼 문자열을 
 * return 하도록 solution 함수를 완성해보세요.
 */
namespace C_Sharp_Cording_Test_Practice
{
    /*
    public class Solution
    {
        public string solution(string my_string, int num1, int num2)
        {
            char[] answer = my_string.ToCharArray();
            char _temp = answer[num1];
            answer[num1] = answer[num2];
            answer[num2] = _temp;
            return new string(answer);
        }
    }
    class 인덱스_바꾸기
    {
        static void Main(string[] args)
        {
            Solution _solution = new Solution();

            string _myString01 = "hello";
            string _myString02 = "I love you";

            foreach (var str in _solution.solution(_myString01, 1, 2))
            {
                Console.Write(str + " ");
            }
            Console.WriteLine();
            foreach (var str in _solution.solution(_myString02, 3, 6))
            {
                Console.Write(str + " ");
            }
            Console.WriteLine();
        }
    }
    */
}
