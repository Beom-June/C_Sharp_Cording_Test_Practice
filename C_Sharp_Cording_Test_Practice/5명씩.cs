using System;
using System.Collections.Generic;

/* 최대 5명씩 탑승가능한 놀이기구를 타기 위해 줄을 서있는 
 * 사람들의 이름이 담긴 문자열 리스트 names가 주어질 때, 
 * 앞에서 부터 5명씩 묶은 그룹의 가장 앞에 서있는 사람들의 이름을 담은 리스트를 
 * return하도록 solution 함수를 완성해주세요. 
 * 마지막 그룹이 5명이 되지 않더라도 가장 앞에 있는 사람의 이름을 포함합니다.
 */
namespace C_Sharp_Cording_Test_Practice
{
    /*
    public class Solution
    {
        public string[] solution(string[] names)
        {
            List<string> answer = new List<string>();

            for (int i = 0; i < names.Length; i += 5)
            {
                answer.Add(names[i]);
            }
            return answer.ToArray();
        }
    }
    class _5명씩
    {
        static void Main(string[] args)
        {
            Solution _solution = new Solution();

            string[] _names = { "nami", "ahri", "jayce", "garen", "ivern", "vex", "jinx" };

            foreach (var item in _solution.solution(_names))
            {
                Console.Write(item + " ");
            }
        }
    }
    */
}