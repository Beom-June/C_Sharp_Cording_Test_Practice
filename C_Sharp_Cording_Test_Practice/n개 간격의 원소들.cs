using System;
using System.Collections.Generic;

/* 정수 리스트 num_list와 정수 n이 주어질 때, 
 * num_list의 첫 번째 원소부터 마지막 원소까지 n개 간격으로 저장되어있는 
 * 원소들을 차례로 담은 리스트를 return하도록 solution 함수를 완성해주세요.
 */
namespace C_Sharp_Cording_Test_Practice
{
    /*
    public class Solution
    {
        public int[] solution(int[] num_list, int n)
        {
            List<int> answer = new List<int>();
            for (int i = 0; i < num_list.Length; i += n)
            {
                answer.Add(num_list[i]);
            }
            return answer.ToArray();
        }
    }
    class n개_간격의_원소들
    {
        static void Main(string[] args)
        {
            Solution _solution = new Solution();

            int[] _numList = { 4, 2, 6, 1, 7, 6 };

            foreach (var item in _solution.solution(_numList, 2))
            {
                Console.Write(item + " ");
            }
            Console.WriteLine();

            foreach (var item in _solution.solution(_numList, 4))
            {
                Console.Write(item + " ");
            }
            Console.WriteLine();
        }
    }
    */
}