using System;
using System.Collections.Generic;

/*정수 n과 정수 배열 numlist가 매개변수로 주어질 때, 
 * numlist에서 n의 배수가 아닌 수들을 제거한 배열을 
 * return하도록 solution 함수를 완성해주세요.
 */
namespace C_Sharp_Cording_Test_Practice
{
    /*
    public class Solution
    {
        public int[] solution(int n, int[] numlist)
        {
            List<int> _answer = new List<int>();

            for (int i = 0; i < numlist.Length; i++)
            {
                if(numlist[i] % n == 0)
                {
                    _answer.Add(numlist[i]);
                }
            }
            return _answer.ToArray();
        }
    }
    class n의_배수_고르기
    {
        static void Main(string[] args)
        {
            Solution _solution = new Solution();

            int[] _list01 = { 4, 5, 6, 7, 8, 9, 10, 11, 12 };
            int[] _list02 = { 1, 9, 3, 10, 13, 5 };
            int[] _list03 = { 2, 100, 120, 600, 12, 12 };

            Console.WriteLine(String.Join(" ", _solution.solution(3, _list01)));
            Console.WriteLine(String.Join(" ", _solution.solution(3, _list02)));
            Console.WriteLine(String.Join(" ", _solution.solution(3, _list03)));
        }
    }
    */
}