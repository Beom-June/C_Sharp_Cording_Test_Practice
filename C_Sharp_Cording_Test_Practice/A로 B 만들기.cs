using System;

/* 문자열 before와 after가 매개변수로 주어질 때, 
 * before의 순서를 바꾸어 after를 만들 수 있으면 1을, 
 * 만들 수 없으면 0을 return 하도록 solution 함수를 완성해보세요.
 */
namespace C_Sharp_Cording_Test_Practice
{
    /*
    public class Solution
    {
        public int solution(string before, string after)
        {
            char[] _arrBefore = before.ToCharArray();
            char[] _arrAfter = after.ToCharArray();

            Array.Sort(_arrBefore);
            Array.Sort(_arrAfter);

            string _sortedBefore = new string(_arrBefore);
            string _sortedAfter = new string(_arrAfter);

            if (_sortedBefore == _sortedAfter)
                return 1;
            else
                return 0;
        }
    }
    class A로_B_만들기
    {
        static void Main(string[] args)
        {
            Solution _solution = new Solution();

            string _before01 = "olleh";
            string _before02 = "allpe";

            string _after01 = "hello";
            string _after02 = "apple";

            Console.WriteLine(_solution.solution(_before01, _after01));     //  1
            Console.WriteLine(_solution.solution(_before02, _after02));     //  0
        }
    }
    */
}