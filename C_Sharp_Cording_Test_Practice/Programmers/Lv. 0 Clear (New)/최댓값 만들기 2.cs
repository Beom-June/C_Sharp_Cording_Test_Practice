using System;

/* 정수 배열 numbers가 매개변수로 주어집니다. 
 * numbers의 원소 중 두 개를 곱해 만들 수 있는 최댓값을 return하도록 solution 함수를 완성해주세요.
 */
namespace C_Sharp_Cording_Test_Practice
{
    /*
    public class Solution
    {
        public int solution(int[] numbers)
        {
            int answer = 0;
            Array.Sort(numbers);

            int _maxMultiply = numbers[numbers.Length - 1] * numbers[numbers.Length - 2];
            int _minMultiply = numbers[0] * numbers[1];

            answer = Math.Max(_maxMultiply, _minMultiply);
            return answer;
        }
    }
    class 최댓값_만들기_2
    {
        static void Main(string[] args)
        {
            Solution _solution = new Solution();

            int[] _numbers01 = { 1, 2, -3, 4, -5 };
            int[] _numbers02 = { 0, -31, 24, 10, 1, 9 };
            int[] _numbers03 = { 10, 20, 30, 5, 5, 20, 5 };

            Console.WriteLine(_solution.solution(_numbers01));
            Console.WriteLine(_solution.solution(_numbers02));
            Console.WriteLine(_solution.solution(_numbers03));
        }
    }
    */
}
