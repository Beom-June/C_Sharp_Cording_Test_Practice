using System;
using System.Collections.Generic;

/* Retry
 * 문자열 myString이 주어집니다. 
 * 
 * myString을 문자 "x"를 기준으로 나눴을 때 나눠진 문자열 각각의 길이를 
 * 순서대로 저장한 배열을 return 하는 solution 함수를 완성해 주세요.
 */
namespace C_Sharp_Cording_Test_Practice
{
    /*
    public class Solution
    {
        public int[] solution(string myString)
        {
            List<int> answer = new List<int>();
            string[] _split = myString.Split('x');

            foreach (var str in _split)
            {
                answer.Add(str.Length);
            }
            return answer.ToArray();
        }
    }
    class x_사이의_개수
    {
        static void Main(string[] args)
        {
            Solution _solution = new Solution();

            string _myString01 = "oxooxoxxox";
            string _myString02 = "xabcxdefxghi";

            foreach (var item in _solution.solution(_myString01))
            {
                Console.Write(item + " ");
            }
            Console.WriteLine();

            foreach (var item in _solution.solution(_myString02))
            {
                Console.Write(item + " ");
            }
            Console.WriteLine();
        }
    }
    */
}
